#!/usr/bin/env bash

ensure_swiftenv_installed() {
  if [ ! -f "$(swiftenv_path)" ]; then
    download_swiftenv
  fi
}

swiftly_path() {
  echo "Downloading Swiftly..."
  local swiftenv_url
  curl -o swiftly  https://github.com/swiftlang/swiftly/releases/latest/download/swiftly-aarch64-unknown-linux-gnu
}

download_swiftly() {

}